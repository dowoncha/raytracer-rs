extern crate raytracer;

// use raytracer::scene::Scene;

fn main() {
    println!("Hello spheres!");

    // let scene = Scene::new();

    // White material
    // let w_material = Materia::new().color()

    // scene.add_material(w_material)

    // let plane = Plane::new()
    //     .position(Vec3f::new(0.0, -2.0, 0.0))
    //     .normal(Vec3f::new(0.0, 1.0, 0.0))
    //     .material("White");

    // scene.add_object(plane)

    // let image = render(&scene)

    // Draw image
}
